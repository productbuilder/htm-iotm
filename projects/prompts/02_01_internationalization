You are working in an existing Node.js repository.

Task:
Update the already implemented app in `src/app` to support internationalization (i18n) for all user-facing text inside the widget.

Important goal:
All technical code, file names, component names, variable names, comments, architecture terms, and implementation details must remain in English.
However, the visible UI text inside the widget must become language-configurable, with support for:
- English
- Dutch

The widget must be able to choose the correct text set based on a language/locale setting.

Context:
The current app already exists and includes:
- `iotm-participation-widget`
- Shadow DOM shell
- step-based flow
- upload, metadata, consent, contact, review, confirmation
- config handling
- API client
- validators
- CSS theming
- English-only UI text

This task is not a rewrite.
This is an incremental refactor of the existing app to add proper text localization.

Important repository convention:
Keep the existing flat Productbuilder-style structure in `src/app`.
Do not introduce nested directories unless absolutely necessary.
Prefer updating the current structure.

Current structure includes:
src/app/
- index.html
- index.js
- app.js
- config.js
- components/
- css/
- js/
- img/

General implementation requirement:
Introduce a dedicated external text/settings layer for UI copy so the widget can render in English or Dutch without hardcoded visible strings in the component logic.

Functional requirement:
The widget must support language switching based on:
1. explicit widget configuration via attribute/property, and
2. language/locale code

At minimum:
- `locale="en"` or `locale="en-GB"` or `locale="en-US"` => use English
- `locale="nl"` or `locale="nl-NL"` or `locale="nl-BE"` => use Dutch
- unknown locale => fallback to English

You may also use `lang` if that fits the current design, but keep compatibility with the existing `locale` attribute as the primary option.

What needs to change:
1. Move all visible UI copy out of the component implementation into an external translation/settings file.
2. Add a simple i18n layer that:
   - resolves active language from locale
   - exposes translation lookup
   - supports nested translation keys if useful
   - has a safe fallback to English
3. Refactor the widget so that all visible text is read from the i18n layer instead of being hardcoded.
4. Ensure the full app flow works in both English and Dutch.
5. Keep all technical implementation language in English.

What counts as visible UI text:
- headings
- step labels
- button text
- field labels
- helper text
- instructions
- placeholders if present
- validation messages
- upload messages
- empty states
- error messages
- modal titles/content
- review labels
- confirmation messages
- backend unavailable messages
- success states
- accessibility-only visible text or aria labels where user-facing wording matters

What should stay in English:
- code
- file names
- class names
- function names
- comments
- documentation comments
- config object keys where technical
- event names
- CSS part names
- CSS variable names
- API naming

Recommended implementation approach:
Add an external translations file and a small i18n helper.

Preferred structure:
- keep `config.js` for technical defaults/config
- add one new flat file for UI text definitions, for example:
  - `src/app/js/i18n.js`
  or
  - `src/app/js/translations.js`

You may also add one helper file if needed, but keep it simple and flat.

Recommended behavior:
- translation dictionaries for `en` and `nl`
- `resolveLanguageFromLocale(locale)` helper
- `createTranslator(language)` or equivalent helper
- `t(key, params?)` translation function
- fallback to English if a key is missing in Dutch
- fallback to key or safe default if missing in both, but prefer complete dictionaries

Refactor expectations:
- remove hardcoded user-facing strings from `app.js` and subcomponents
- use translation keys consistently
- keep rendering logic readable
- do not over-engineer for many languages yet
- design it so more languages could be added later without rewriting the widget

Attribute/property behavior:
Support language selection through existing config flow.
At minimum:
- existing `locale` attribute/property should drive the selected language
- if `locale` changes after initialization and the component already supports attribute reactivity, re-render with the new language
- if no locale is provided, default to English

Language mapping rules:
- locales starting with `en` => English
- locales starting with `nl` => Dutch
- anything else => English

Content scope to translate:
At minimum translate all current widget flow text:
- Introduction
- Upload media
- Description / metadata
- Rights and consent
- Contact details
- Review and confirmation
- buttons like Next, Back, Submit, Close, Open, Remove, Retry if present
- validation messages
- upload instructions
- drag and drop instructions
- review labels
- success screen
- API/network status messages
- modal content related to rights/contact/privacy/public use

Demo/update expectations:
Update `src/app/index.html` so it demonstrates at least:
- one widget instance in English
- one widget instance in Dutch
or
- one widget instance with clear instructions on how to switch locale via attribute

Use the approach that best fits the current demo page, but make the i18n behavior obvious and testable.

Accessibility:
Make sure translated text also applies where relevant to:
- `aria-label`
- modal close labels
- status text
- validation feedback
- step navigation labels

Validation and messages:
Ensure validation errors appear in the active language.
Ensure upload/type/size errors appear in the active language.
Ensure backend error messages shown to users are localized where they are app-defined.

Do not:
- introduce a third-party i18n library unless already present in the repo
- create nested locale folders
- convert the whole app into a framework
- add unnecessary abstractions
- leave mixed hardcoded English strings in the UI

Do:
- make the implementation small, practical, and maintainable
- keep the translation structure clear
- keep English as the source/fallback language
- ensure Dutch translations are natural and complete

Suggested files to inspect/update:
- `src/app/app.js`
- `src/app/config.js`
- `src/app/index.html`
- `src/app/components/modal.js`
- `src/app/js/helpers.js`
- `src/app/js/validators.js`
- `src/app/js/api-client.js`
- add `src/app/js/i18n.js` or `src/app/js/translations.js` if needed

Implementation detail suggestions:
- Keep technical config separate from UI copy
- It is acceptable to store translation dictionaries as plain objects
- It is acceptable to use nested keys such as:
  - `steps.introduction`
  - `buttons.next`
  - `validation.emailInvalid`
  - `upload.dropzoneTitle`
- If interpolation is useful, support simple placeholders such as `{maxSize}`

Examples of desired behavior:
- `<iotm-participation-widget locale="en"></iotm-participation-widget>` renders English UI
- `<iotm-participation-widget locale="nl"></iotm-participation-widget>` renders Dutch UI
- `<iotm-participation-widget locale="nl-NL"></iotm-participation-widget>` renders Dutch UI
- `<iotm-participation-widget locale="fr"></iotm-participation-widget>` falls back to English UI

Important output requirements:
- Implement the i18n support directly in code
- Reuse the existing app structure
- Keep all code and technical naming in English
- Externalize all visible UI strings
- Make English and Dutch both fully supported
- Keep the solution lightweight
- Run basic validation checks after refactoring

What I want from you:
1. Inspect the current implementation in `src/app`
2. Add a translation layer
3. Refactor the widget to use it
4. Update the demo page to show or document locale switching
5. Run relevant checks
6. Summarize the files changed and how locale selection works

Now implement internationalization for the existing widget.