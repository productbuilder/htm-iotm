Review the current internationalization implementation in `src/app` and perform a full cleanup/verification pass.

Goal:
Ensure the widget has complete, consistent, production-ready i18n support for English and Dutch, with no remaining hardcoded user-facing strings in the widget UI.

What to do:
1. Inspect all files in `src/app` related to the widget.
2. Find any remaining hardcoded visible UI strings in:
   - `app.js`
   - subcomponents
   - modal code
   - validators
   - helper-generated UI text
   - demo page text where relevant
3. Move any remaining visible UI copy into the i18n/translations layer.
4. Verify that all of these are localized:
   - step labels
   - headings
   - buttons
   - field labels
   - placeholders
   - helper text
   - validation messages
   - upload instructions
   - drag/drop messages
   - file size/type errors
   - API/network status messages
   - modal titles/content
   - review labels
   - confirmation/success messages
   - aria-label text and accessibility-facing copy where user-visible wording matters
5. Verify locale resolution behavior:
   - `en`, `en-GB`, `en-US` => English
   - `nl`, `nl-NL`, `nl-BE` => Dutch
   - unknown locale => English fallback
6. Verify missing-key fallback behavior:
   - Dutch missing key falls back to English
   - missing in both does not break rendering
7. Verify that changing the `locale` attribute after initialization re-renders the widget correctly if attribute reactivity is supported.
8. Verify that the demo page clearly shows how to test both languages.
9. Keep all technical code and naming in English.
10. Keep the solution lightweight and flat. Do not add libraries.

Implementation requirements:
- Do not rewrite the widget architecture unnecessarily.
- Keep the existing structure in `src/app`.
- Only make targeted fixes and cleanup improvements.
- If you find repeated translation access patterns that can be simplified cleanly, improve them without over-engineering.

Validation:
- Run relevant checks after updates.
- Summarize:
  - files changed
  - any remaining limitations
  - how locale selection works
  - whether any hardcoded UI strings remain

Success criteria:
- All user-facing widget text is fully externalized
- English and Dutch both work correctly
- Fallback behavior is correct
- No mixed-language UI remains unless intentionally documented
- The widget remains maintainable and simple

Now perform the review, fix any issues found, and report the result.